@model Query

@{
    ViewData["Title"] = "Результат";
}

<h1>Результат</h1>
<hr />

@if (string.IsNullOrEmpty(Model.Error))
{
    @if (Model.QueryId.Equals("S1"))
    {
        <p style="font-size:20px">
            <b>Середня ціна авто марки @Model.BrandName:</b> @decimal.Round(Model.AvgPrice, 2, MidpointRounding.AwayFromZero)
        </p>
    }
    else if (Model.QueryId.Equals("S2"))
    {
        <p style="font-size:20px">
            <b>Користувачі, що придбали хоча б одне авто марки @Model.BrandName</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Ім'я
                    </th>
                    <th>
                        Прізвище
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.PersonsNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.PersonsNames[i]
                        </td>
                        <td>
                            @Model.PersonsSurnames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("S3"))
    {
        <p style="font-size:20px">
            <b>Назви та ціни всіх авто усіх марок країни @Model.CountryName</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Назва
                    </th>
                    <th>
                        Ціна
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.CarNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.CarNames[i]
                        </td>
                        <td>
                            @decimal.Round(Model.CarPrices[i], 2, MidpointRounding.AwayFromZero)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("S4"))
    {
        <p style="font-size:20px">
            <b>Назви марок усіх авто, придбаних користувачем з ім'ям @Model.PersonName, прізвищем @Model.PersonSurname та email-ом @Model.PersonEmail</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Назва
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.BrandNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.BrandNames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("S5"))
    {
        <p style="font-size:20px">
            <b>Назви марок, що продають авто ціною не @Model.Price у.о.</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Назва
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.BrandNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.BrandNames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("S6"))
    {
        <p style="font-size:20px">
            <b>Назви марок, які не продають авто з назвою @Model.CarName</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Назва
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.BrandNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.BrandNames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("A1"))
    {
        <p style="font-size:20px">
            <b>Назви країн, марки з яких продають авто принаймні за всіма тими цінами, що й марка із кодом @Model.BrandId</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Назва
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.CountryNames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.CountryNames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("A2"))
    {
        <p style="font-size:20px">
            <b>Прізвища користувачів, які придбали точно ті ж самі авто, що й покупець з email @Model.PersonEmail</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Прізвище
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.PersonsSurnames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.PersonsSurnames[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("A3"))
    {
        <p style="font-size:20px">
            <b>Прізвища та email-и користувачів з іменем @Model.PersonName, які придбали всі наявні авто</b>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Прізвище
                    </th>
                    <th>
                        Email
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.PersonsSurnames.Count; i++)
                {
                    <tr>
                        <td>
                            @Model.PersonsSurnames[i]
                        </td>
                        <td>
                            @Model.PersonsEmails[i]
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else if (Model.QueryId.Equals("T1"))
    {

    }
    else if (Model.QueryId.Equals("T2"))
    {

    }
}
else
{
    <p style="font-size:20px">
        Результат запиту порожній.
        <br />
        Будь ласка, спробуйте ще раз з іншим параметром.
    </p>
}

<table>
    <tr>
        <td>
            <form method="get" asp-controller="Queries" asp-action="Index">
                <input type="submit" class="btn btn-primary" value="Назад до запитів" />
            </form>
        </td>
        <td></td>
        <td></td>
        <td></td>
        <td>
            <form method="get" asp-controller="Home" asp-action="Index">
                <input type="submit" class="btn btn-primary" value="На головну" />
            </form>
        </td>
    </tr>
</table>


<div class="modal fade" id="error" tabindex="-1" role="alertdialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Повідомлення</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="errorText">
                @Model.Error
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">ОК</button>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    <script>
        window.addEventListener("load", function () {
            if ('@Model.ErrorFlag' == 1) {
                $("#error").modal();
            }
        });
    </script>
}